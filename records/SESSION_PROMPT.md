# Session Prompt

Next session continuation for nix-vps security work.

## What was done in the 2026-03-28 session

- Added `nixos/modules/hardening.nix`: kernel hardening (sysctl), auditd, protectKernelImage — deployed and active
- Added `nixos/modules/crowdsec.nix`: CrowdSec agent + nftables firewall bouncer — deployed and active
- Wired both into `flake.nix` via `nixpkgs-unstable` overlay + module import (modules not in nixpkgs 25.05)
- Exposed CrowdSec Prometheus metrics on `10.0.0.10:6060` (WireGuard interface)
- Opened port 6060 on wg0 firewall in `nix-vps-private/modules/vps-host.nix`
- Added CrowdSec scrape job to Prometheus (pi-node-b) via `extraStaticJobs`
- Added CrowdSec Grafana dashboard to pi-node-b — provisioned and visible
- All services verified active: `crowdsec`, `crowdsec-firewall-bouncer`, `auditd`
- Prometheus scraping CrowdSec at `10.0.0.10:6060`, target health: UP
- Investigation file written: `nix-vps-private/records/crowdsec/INVESTIGATION.md`

## Next session task: CrowdSec alerting

**Goal:** get notified (email + Telegram via existing Alertmanager) when CrowdSec detects an attack.

### What needs to happen

1. **Add `extraAlertRules` option to `nix-services/services/prometheus/options.nix`**
   - New option: `scrape.extraAlertRules` — list of strings, appended to `alert.rules.yml`
   - Wire into `config-text.nix` in the `alertRulesText` generation

2. **Add CrowdSec alert rules in `nix-pi-private/modules/pi-node-b.nix`** via the new option:
   - `CrowdSecTrackingActivity` — fires when `cs_bucket_created_total` rate increases significantly (sustained attack incoming)
   - `CrowdSecDown` — fires when `up{job="crowdsec"} == 0` for >2m (agent stopped)
   - Consider: `CrowdSecHighBucketCount` — fires when `sum(cs_buckets)` exceeds a threshold (many IPs tracked simultaneously)

3. **Note on `cs_decisions_count`**: this metric is not present in crowdsec 1.6.8 — only bucket/parse metrics available. Alerting on `cs_bucket_created_total` rate is the best proxy for "ban triggered".

4. **Deploy:** `nix-services` commit + push, then update `nix-pi` flake.lock, then rebuild pi-node-b.

5. **Verify:** confirm alert rule appears in Prometheus UI under `/alerts`, fire a test or wait for organic activity.

### Key facts to remember

- Alertmanager is on pi-node-b, sends to `contact@primary.example` (email) and Telegram
- Alert rules are in a single file `/etc/prometheus/alert.rules.yml`, generated by `nix-services`
- `nix-services` is a separate repo at `../nix-services` — changes there need their own commit/push + flake.lock update in `nix-pi`
- pi-node-b must be rebuilt with `--build-host eduardo@pi-node-b --target-host eduardo@pi-node-b` (aarch64, can't build locally)
- CrowdSec metrics endpoint: `http://10.0.0.10:6060/metrics` — verify still reachable from pi-node-b at session start
